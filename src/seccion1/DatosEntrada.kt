package seccion1

fun main(args: Array <String>) {

    println("¿Cuál es tu Nombre?");
    val nombre = readLine();
    println(nombre);

    var num = 8;
    println(num++);
    println(--num);

    // ? -> Al agregar esto a una variable, le dice al compilador que puede ser o no nula;
    // !! -> Al agregar esto a una variables, le dice al compilador que tiene que tener un valor obligatoriamente.



}